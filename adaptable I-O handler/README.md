# adaptable I/O handler

Top-level HC subsystem for adaptable interface translation between the hyperconnectome and changing sensory, motor, computational, diagnostic, or host-body channels. `I-O` is used in the path because `/` is a Git path separator.
